import { Stack, styled } from "@mui/material";

export const StackOutlet = styled(Stack)(() => ({
    width: '100%',
    height:'100vh',
    display:'flex',
    justifyContent:'center',
    alignItems:'center',
    textAlign:'center',
  }));
  export const StackForm = styled(Stack)(() => ({
display:'block'
  }));
  export const Container = styled(Stack)(() => ({
padding:'0px 7rem'  }));